#include <bits/stdc++.h>
#define MOD (ll)(1e9+7)
#define MAX(x, y) (x > y ? x : y)
#define MIN(x, y) (x < y ? x : y)
using namespace std;
using ll = long long;

void solve();
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    ll t = 0;
    scanf("%lld", &t);
    while(t--) solve();
    return 0;
}
/////////////////////////////////////////////////////////////////
// CODE HERE
/////////////////////////////////////////////////////////////////
void solve() {
}
